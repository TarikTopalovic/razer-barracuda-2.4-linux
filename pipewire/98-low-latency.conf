# Lower buffer for less audio lag (gaming). Raise quantum if you get crackling.
context.properties = {
    default.clock.quantum     = 512
    default.clock.min-quantum = 256
    default.clock.max-quantum = 1024
}
